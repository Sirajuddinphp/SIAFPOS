# Runtime and migration fixes

- Removed the need for a duplicate KDS column migration.
- Kept the original KDS schema from migration 019 (`kitchen_station`, `priority`, timestamps).
- Updated `KotRepository` to use the actual `kitchen_station` database column while preserving the renderer-facing `stationCode` contract.
- Restored `tests/migration-runner.test.ts` to a valid Vitest suite.
- Migration expectation is 24 and validates the KDS columns.
- Do not add a separate `025_add_kds_columns...` migration to this source; those columns already exist in migration 019.
