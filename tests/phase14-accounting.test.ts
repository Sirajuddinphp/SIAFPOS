import { describe, expect, it } from "vitest";
import { AccountingService } from "../electron/services/accounting-service";
import { createMigratedTestDatabase, seedAuthFixture } from "./test-helpers";
describe("phase 14 accounting",()=>{it("posts income and expense entries",()=>{const db=createMigratedTestDatabase();const {user}=seedAuthFixture(db);const service=new AccountingService(db);let dashboard=service.dashboard();const income=dashboard.accounts.find(a=>a.accountType==="income")!;const expense=dashboard.accounts.find(a=>a.accountType==="expense")!;service.createEntry({entryDate:new Date().toISOString(),entryType:"income",accountUuid:income.uuid,amountMinor:100000,description:"Sales income"},user.uuid);service.createEntry({entryDate:new Date().toISOString(),entryType:"expense",accountUuid:expense.uuid,amountMinor:25000,description:"Gas expense"},user.uuid);dashboard=service.dashboard();expect(dashboard.summary.incomeMinor).toBe(100000);expect(dashboard.summary.expenseMinor).toBe(25000);expect(dashboard.summary.netProfitMinor).toBe(75000);db.close();});});
