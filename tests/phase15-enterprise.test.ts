import { describe,expect,it } from "vitest";
import { EnterpriseService } from "../electron/services/enterprise-service";
import { createMigratedTestDatabase,seedAuthFixture } from "./test-helpers";
describe("phase 15 enterprise",()=>{it("activates a license, registers devices and creates API keys",()=>{const db=createMigratedTestDatabase();const {user,terminal}=seedAuthFixture(db);const s=new EnterpriseService(db);const license=s.activateLicense({licenseKey:"SIAF-TEST-LICENSE-123",planCode:"pro",maxOutlets:5,maxTerminals:20,expiresAt:null},user.uuid);expect(license.status).toBe("active");const d=s.registerDevice({terminalUuid:terminal.uuid,deviceName:"Counter 1",deviceFingerprint:"fingerprint-123456",platform:"win32",appVersion:"0.1.0"},user.uuid);expect(d.status).toBe("active");const key=s.createApiKey({name:"Reports",scopes:["reports:read"],expiresAt:null},user.uuid);expect(key.secret).toMatch(/^siaf_/);expect(s.dashboard().summary.activeApiKeys).toBe(1);db.close();});});
